node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
.env
.env.local
.env.*.local
.vercel

# Replit-specific files (no longer in use)
.replit
replit.md

# Sensitive / credential files — never commit these
cookies.txt
prod_cookies.txt
*.cookies.txt
production-migration.sql
*-migration-prod.sql

# Claude Code session data (worktrees, transcripts)
.claude/
